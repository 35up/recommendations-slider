{
  "name": "recos-slider--monorepo",
  "private": true,
  "scripts": {
    "bump-version": "lerna version --no-git-tag-version --no-push",
    "bootstrap": "lerna bootstrap --no-ci",
    "build": "lerna run build --no-ci",
    "test": "lerna run test --no-ci"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}
